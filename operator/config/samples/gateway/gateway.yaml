apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: inference-gw
spec:
  gatewayClassName: istio          # or gke, kong‑gateway, etc.
  listeners:
    - name: http
      protocol: HTTP
      port: 80
